---
- hosts: nginx-server
  become: true
  gather_facts: true
  name: setup nginx as a load balancer  
  roles:
    - { role: roles/preparing, become: True, tags: preparing }
    - { role: roles/nginx, become: True, tags: nginx }
